# Phase 5 — Pilot checklist: approvals still required

Date: 17 September 2026 · Task `p5-readiness` · Brief §15 Phase 5 ("obtain explicit approval for the pilot cohort, production financial configuration and migration/cutover"), §18 decisions, §2.3 non-goals, §14.2.

**Status of every item below: NOT GRANTED.** Nothing on `feat/admin-redesign` is deployed, no migration is applied, no flag is set, no provider has been called. Each item names the owner (Louis unless stated), what "granted" must look like (a dated entry in the decision register with evidence), and what it unblocks. The website release workflow in `AGENTS.md` (commit, push, deploy without asking) covers **routine website changes**; it does not cover any item here — these need a fresh, explicit approval.

## A. Brief §18 decisions (commercial policy)

| #     | Decision                                                                                           | Current state (Phase 0 §9, Phases 1–4)                                                                                | Blocks                                                        | Status          |
| ----- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | --------------- |
| 18.1  | Final recurring tiers / prices / inclusions after calculator review                                | Every catalogue item is `draft`, `chargeable: false`, `draft_sandbox`; fixture prices labelled sandbox                | Publishing any package; issuing a service schedule            | **NOT GRANTED** |
| 18.2  | Internal loaded rates, margin targets, contingency method, approval thresholds                     | `POLICY_2026_09_DRAFT` (state `draft`, every constant hypothetical)                                                   | Quote issuance from the estimator                             | **NOT GRANTED** |
| 18.3  | £600 handover fee tax basis, due date, exact scope, exceptions, later offboarding                  | `tax_basis = 'pending'` / `tax_code_ref = 'pending_decision'` everywhere; `handover_schedules` CHECK blocks issue     | Issuing a handover schedule or handover obligation            | **NOT GRANTED** |
| 18.4  | Application fee after independent handover; removing operational dependence                        | `fee_disposition = 'pending'` blocks issue; Suffolk's signed form untouched                                           | Handover issuance                                             | **NOT GRANTED** |
| 18.5  | Who pays / operates between launch, acceptance and the Managed billing date; maximum gap           | Not modelled; `billing_start_arrangement = 'unresolved'` is the explicit state                                        | Billing activation                                            | **NOT GRANTED** |
| 18.6  | Warranty duration / start, partial acceptance, disputes, client delays                             | `allowPartial` defaults off; warranty not modelled                                                                    | Opening the chooser on a partial acceptance                   | **NOT GRANTED** |
| 18.7  | Recurring billing unit for multi-system clients; shared-cost allocation                            | Partial unique on `(tenant_id, coalesce(project_id, tenant_id))` supports either answer                               | Billing activation for a second system                        | **NOT GRANTED** |
| 18.8  | Revolut–Xero feed, Xero primary-invoice configuration, accountant-approved tax / clearing mappings | Create still uses `NoTax` / `XERO_TAX_TYPE`; `TaxSnapshot` is a caller input                                          | Any obligation leaving `pending_decision`; `xero.*` handlers  | **NOT GRANTED** |
| 18.9  | Initial mandate timing, charge notice, scheduling behaviour verified against current provider docs | `start_date` + Idempotency-Key implemented; `providerLeadDays` is an input, not a constant; consent wording undefined | `gocardless.schedule_activation`                              | **NOT GRANTED** |
| 18.10 | Deposit / milestone templates, discounts, cancellation, refunds, retry, suspension                 | Milestone split is an input to `createObligationsFromOrderForm`; no refund handler by design                          | Obligation creation from a template; any retry policy         | **NOT GRANTED** |
| 18.11 | Exact staff capabilities; who approves commercial / financial exceptions                           | Binary staff; "approver ≠ author" only; owner text free-form on exceptions                                            | Every "second person" gate in production; staff read policies | **NOT GRANTED** |
| 18.12 | Approved pilot client / cohort and production cutover authority                                    | Three legacy tenants excluded by rule; no other client identified                                                     | Everything in sections B–D                                    | **NOT GRANTED** |

## B. Pilot cohort (brief §14.2 step 4, §15 Phase 5)

| Item                                                                                                                                   | Evidence required                                                                        | Status          |
| -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------- |
| B1. Named pilot client(s) — **new** client / new service journey only; never The Dance Exclusive, Suffolk Tennis or New Future Therapy | Decision register entry naming the tenant id and the journey (Managed or Independent)    | **NOT GRANTED** |
| B2. Pilot scope — which flags, which environment (preview + branch database first), which provider environment (sandbox first)         | Written scope with the flag list and dates                                               | **NOT GRANTED** |
| B3. Two staff users exist with memberships and TOTP enrolled (author ≠ approver, reviewer ≠ issuer)                                    | `memberships` rows on the internal tenant; MFA verified; N-a loop fix cherry-picked      | **NOT GRANTED** |
| B4. Pilot client signatory identified and minted as `client_admin` through the invite path, not the GET auto-join                      | Membership row; invite audit entry                                                       | **NOT GRANTED** |
| B5. Exit criteria — what "observed real exception handling" means before enabling further automation                                   | At least one dead letter and one owned exception resolved with evidence in the pilot log | **NOT GRANTED** |
| B6. Email allowlist for non-production — no fixture or preview may email a real client                                                 | `sendEmail.ts` allowlist or Resend sandbox domain in place (Phase 0 §7 risk)             | **NOT GRANTED** |

## C. Production financial configuration

| Item                                                                                                                                                                                 | Evidence required                                                                                                                       | Status          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| C1. `GOCARDLESS_ENVIRONMENT` value confirmed and matched to the webhook endpoint configured at GoCardless (Phase 0 §11: value not visible)                                           | Read from the running app's Sandbox/Live badge; GoCardless dashboard screenshot of the endpoint                                         | **NOT GRANTED** |
| C2. Which collection orchestrator owns each obligation kind (`xero_native` vs `nullshift_gocardless` vs `stripe` vs `manual`) after auditing Xero-connected payment services (§10.1) | Decision register entry; Xero payment-services screenshot                                                                               | **NOT GRANTED** |
| C3. Retire or gate the Stripe card rail for care plans (N-c) before any new activation rule is enabled                                                                               | Code change merged or explicit acceptance of the risk                                                                                   | **NOT GRANTED** |
| C4. Suffolk Tennis' open incomplete billing request (N-i): cancel and re-issue or leave as legacy — never automatic                                                                  | Decision; with `integrationWorkers` on, a `billing_requests.fulfilled` for it would be held and open an exception instead of activating | **NOT GRANTED** |
| C5. Freeze `NSI_v1_2026_08` prices per legacy tenant before any v2 constant changes (N-d); hand-set `tenants.legacy_model` for TDE, STL, NFT (N-g)                                   | Per-tenant snapshot stored; marker set (the invariants script prints `legacy_model`)                                                    | **NOT GRANTED** |
| C6. Xero tax / clearing account codes approved by the accountant (18.8); `XERO_PAYMENT_ACCOUNT_CODE` confirmed                                                                       | Accountant's written confirmation                                                                                                       | **NOT GRANTED** |
| C7. Production `OPS_V2_FLAGS` value and who may change it                                                                                                                            | Vercel env change recorded with date and approver                                                                                       | **NOT GRANTED** |
| C8. Fix the misspelt `NEXT_PUBLC_STRIPE_PUBLISHABLE_KEY` before any Stripe.js work (Phase 0 §11)                                                                                     | Env var renamed                                                                                                                         | **NOT GRANTED** |

## D. Migration and cutover authority

| Item                                                                                                                                                                                                                                                            | Evidence required                                                                                                          | Status          |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------- |
| D1. Ledger reconciliation (N-h): read `supabase_migrations.schema_migrations`, decide the fate of `060`/`0056`, the two `0020`s and the `0052` names; confirm none of the names `opportunities_quotes` … `finance_exceptions` is already present                | Ledger listing saved with the redesign docs; dry-run re-run with any new names added to its ledger list                    | **NOT GRANTED** |
| D2. Branch-database rehearsal of 0057–0065 in the dry-run's order (0057, 0058, 0059, 0060, 0062, 0063, 0064, 0065); exercise `schedules_guard_frozen_content`, `supersede_service_arrangement`, the 0062 backfill pre-checks and the allocation ceiling trigger | Rehearsal log; `legacy-invariants.sql` before/after diff on the branch copy showing only the documented fields changed     | **NOT GRANTED** |
| D3. Production apply of 0057–0065 (a schema change on project `cweftpoaojwzllzficgt`); each file's ledger entry numbered against the ledger, not the directory                                                                                                  | Explicit go-ahead per file or per batch; before/after invariant snapshots for the three protected tenants stored privately | **NOT GRANTED** |
| D4. Confirmation that no project has two live `build_milestone` invoices and no tenant two live subscriptions (0062 pre-check would abort; 0064 index tolerates it)                                                                                             | Read-only query result                                                                                                     | **NOT GRANTED** |
| D5. Regenerate `packages/db/src/types.ts` after apply (not owned by any Phase 1–4 task)                                                                                                                                                                         | Commit                                                                                                                     | **NOT GRANTED** |
| D6. Merge `feat/admin-redesign` to `main` and deploy — separate from applying migrations; flags stay off in production                                                                                                                                          | PR review; preview deployment with screenshots; `legal-guard` passes                                                       | **NOT GRANTED** |
| D7. `newShell` cut-over (retire duplicate legacy workflows with redirects and a rollback path, §14.2 step 6)                                                                                                                                                    | Visual/workflow review of `/admin/next/*`; N2/N3/N11/N12 fixed first                                                       | **NOT GRANTED** |

## E. Explicitly out of scope (brief §2.3) — no approval sought

No public website redesign; no automatic migration of live subscriptions or the Connect account architecture; no outgoing Revolut payments or bank changes; no autonomous AI acceptance, contract sending, price approval, collection or refund; no replacement ledger or payroll; no "completely safe" or merchant-of-record claims; no new charge justified by an administrator editing a plan or estimate.

## F. How to grant an item

Add a row to the decision register (Phase 0 report §9 is the current register) with owner, status, evidence link, effective date and the feature it unblocks; then update this file's Status column in the same commit. An approval that lives only in chat, a task prompt or an agent's summary does not count.
