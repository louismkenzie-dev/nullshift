# Admin redesign v2 — handoff, 17 September 2026

Branch `feat/admin-redesign` (local; commits `07bfbdd` … `cb1b1a3` plus the Phase 5 files). Production is still GitHub `main` @ `2a52c71` (Phase 0 §11). This document consolidates `docs/ADMIN-REDESIGN-PHASE0-2026-09-17.md` and the thirteen Phase 1–4 documents plus the Phase 5 set in `docs/admin-redesign/`. It states what exists, what is switched off, what was never done, and what Louis has to do next. It is not an approval of anything.

**One-line state:** a complete fixture-driven prototype at `/admin/next/*`, a tested pure-logic layer for estimating, quoting, arrangements, acceptance, obligations, activation and integrations, nine additive migrations that are **NOT APPLIED**, seven feature flags that are **all OFF**, 854 passing unit tests, **no deployment, no provider call, no browser verification**, and every commercial decision in brief §18 still open.

## 1. Deliverables by phase

| Phase | Task(s)                                              | Delivered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Document                                                                                                    |
| ----- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 0     | evidence and architecture                            | Route / schema / permission / integration inventory, calculator comparison, concept mapping, state transitions, migration risk register, ten confirmed incompatibilities + fourteen new ones, decision register (18.1–18.12 + N-a…N-j), live config verification (read-only), first prototype slice (`07bfbdd`)                                                                                                                                                                                                                                                                                 | `docs/ADMIN-REDESIGN-PHASE0-2026-09-17.md`                                                                  |
| —     | scaffolding (`e473ea7`)                              | `apps/web/lib/flags.ts` (7 flags, all off), planned nav routes, quote data-layer stub                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                           |
| 1     | p1-finance, p1-agreements, p1-ops, p1-portal (+a11y) | Fixture-only prototypes: Finance (6 tabs, 3 detail routes, all eight §5.6 exception kinds), Agreements (library + document workspace, second-person gate reused), Sales / Delivery / Automations / Settings (pipeline, quotes, discovery, sandbox catalogue with the £600 fee kept separate, work queue with §7 wording, releases, handover, capacity, 16 automations with dry-run, 11 settings sections), phone-width portal preview with 5 fixtures × 3 roles, accessibility notes                                                                                                            | `phase1-finance.md`, `phase1-agreements.md`, `phase1-ops.md`, `phase1-portal.md`, `phase1-accessibility.md` |
| 2     | p2-estimator, p2-quotes, p2-intake                   | Pure versioned estimator (`lib/estimator`, draft policy, draft catalogue, client-safe view); opportunities / quotes / immutable quote versions / approval evidence (0057, `lib/commercial`, `next/quotes/actions.ts`); §7 classifier and coverage, client next actions (0058, `lib/work`, `lib/nextActions`, `/admin/next/delivery/intake`)                                                                                                                                                                                                                                                     | `phase2-estimator.md`, `phase2-quotes.md`, `phase2-intake.md`                                               |
| 3     | p3-arrangements, p3-acceptance                       | Service arrangements, versioned service / handover schedules, acceptance snapshots, `order_forms.commercial_version` (0059, `lib/legal/arrangements.ts`, `lib/legal/acceptanceSnapshot.ts`, `packages/content/src/legal/orderFormV2.ts`, `next/agreements/actions.ts`); explicit build acceptance, checklist and handover tasks, portal task functions (0060, `lib/delivery`, `/portal/checklist`, `/portal/acceptance`, `planChoiceOpenV2`)                                                                                                                                                    | `phase3-arrangements.md`, `phase3-acceptance.md`                                                            |
| 4     | p4-ledger, p4-activation, p4-ops                     | Billing obligations, provider payments, typed allocations with a ceiling trigger, 0013 index replaced safely with a documented backfill (0062); finance exceptions with safe retries that never collect (0065); mandates, activation reservation, per-arrangement subscription guard (0064); integration inbox / outbox with leased claim, handlers for Xero create / allocate (with recovery lookups), GoCardless schedule / cancel, notify stub, GoCardless webhook branch, ops-worker cron (0063, `lib/integrations`, `lib/billing`, `lib/xeroOps.ts`, `api/cron/ops-worker`, `vercel.json`) | `phase4-ledger.md`, `phase4-activation.md`, `phase4-ops.md`                                                 |
| 5     | p5-readiness                                         | Migration dry-run script (`pnpm -C apps/web migrations:dry-run`, 0 errors / 3 explained warnings), legacy invariant snapshot SQL, permissions audit, runbook, pilot checklist (all NOT GRANTED), this handoff, one section each in `docs/OPERATIONS.md` and `supabase/README.md`                                                                                                                                                                                                                                                                                                                | `phase5-readiness.md`, `phase5-permissions-audit.md`, `phase5-runbook.md`, `phase5-pilot-checklist.md`      |
| 6     | expansion after evidence                             | **Not started** (capacity planning, estimator calibration, usage reviews, read-only bank dashboard, AI assistance).                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                                           |

## 2. Routes

All admin routes sit inside the existing `(dashboard)` group, so login → MFA step-up → `requireStaff()` are inherited unchanged; nothing was added to the legacy `AdminNav`; the prototype rail links between them. Every page has `loading.tsx`; every segment root has `error.tsx` (Next 16 `unstable_retry`); empty states are real. All render fixtures unless a flag row says otherwise.

| Route                                                                                                                                                        | Content                                                                                                                                                                                                                            | Flag-on behaviour                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `/admin/next`                                                                                                                                                | Today: four §5.1 metrics, needs-attention queue, approvals, exceptions                                                                                                                                                             | fixtures                                                                                                     |
| `/admin/next/clients`, `/admin/next/clients/[id]`                                                                                                            | Client list (§5.4 columns); workspace with six independent facets and next-action strip                                                                                                                                            | fixtures (next-action strip not yet wired to `listNextActions`)                                              |
| `/admin/next/quotes`                                                                                                                                         | Quote-version listing with per-state actions                                                                                                                                                                                       | `commercialV2`: reads 0057, actions draft → review → approve (author ≠ approver) → issue → next version      |
| `/admin/next/quotes/[id]`                                                                                                                                    | Quote Studio: six steps, estimator review box, scenarios, drivers, validation, client preview totals                                                                                                                               | `calculator` + `commercialV2`: tries the persisted version, falls back to fixture                            |
| `/admin/next/finance` (+ `invoices`, `invoices/[id]`, `collections`, `collections/[id]`, `subscriptions`, `reconciliation`, `exceptions`, `exceptions/[id]`) | Finance overview, invoices with obligation/balance equation, collections with "authorised — not scheduled", subscriptions incl. legacy read-only, payout reconciliation with human-review matches, exceptions with safe-retry text | fixtures; source-note labels only                                                                            |
| `/admin/next/agreements`, `/admin/next/agreements/[id]`                                                                                                      | Library with eight statuses and filters; document workspace with facts, schedules, risk flags, internal review, client preview, audit trail, issue blockers                                                                        | fixtures; the Phase 3 actions exist in `agreements/actions.ts` but no page calls them yet                    |
| `/admin/next/sales` (`?tab=pipeline                                                                                                                          | quotes                                                                                                                                                                                                                             | discovery                                                                                                    | catalogue`), `/admin/next/sales/opportunities/[id]` | Pipeline list/board, quote-version states, discovery, sandbox catalogue + separate £600 card; opportunity detail with duplicate-match review | fixtures                                                                                                                                                  |
| `/admin/next/delivery` (`?tab=projects                                                                                                                       | queue                                                                                                                                                                                                                              | releases                                                                                                     | handover                                            | capacity`), `/admin/next/delivery/projects/[id]`                                                                                             | Projects with gates and audited waiver, §7 work queue with exact coverage wording, releases incl. partial acceptance, §8.5 handover, capacity assumptions | fixtures |
| `/admin/next/delivery/intake`                                                                                                                                | Classifier what-if, worked examples, mixed-request split, next-action strip                                                                                                                                                        | `workIntake`: record / split / set-next-action forms post to real actions                                    |
| `/admin/next/automations`                                                                                                                                    | 16 §11 workflows, dry-run preview, inert global pause                                                                                                                                                                              | fixtures                                                                                                     |
| `/admin/next/settings`                                                                                                                                       | 11 §5.9 sections with read/change permissions, version history, feature-flag list                                                                                                                                                  | reads `activeFlags()` for display only                                                                       |
| `/admin/next/portal` (+ `checklist`, `checklist/[step]`, `next-steps`, `billing`, `help`; `?client=…&role=…&wide=1`)                                         | Phone-width client-portal preview                                                                                                                                                                                                  | fixtures                                                                                                     |
| `/portal/checklist`, `/portal/acceptance` (real portal)                                                                                                      | Derived read-only checklist; agreed scope and acceptance page                                                                                                                                                                      | `acceptanceGate`: reads 0060, signatory records acceptance, clients move own tasks, staff publish checklists |
| `POST /api/gocardless/webhook`                                                                                                                               | Legacy handler                                                                                                                                                                                                                     | `integrationWorkers`: inbox capture → 200 → `after()` processing from authoritative resource reads           |
| `GET /api/cron/ops-worker` (every 10 min, `CRON_SECRET`)                                                                                                     | `{ ok: true, skipped: "flag_off" }`                                                                                                                                                                                                | `integrationWorkers`: sweep inbox, `runOnce` the outbox                                                      |

## 3. Flags (`OPS_V2_FLAGS`, server-side, all OFF)

| Flag                 | Consumers                                                                           | Needs migrations         | Note                                                                |
| -------------------- | ----------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------- |
| `newShell`           | none yet                                                                            | —                        | reserved for the cut-over; prototype is reachable by URL regardless |
| `commercialV2`       | quotes listing/actions, agreements actions, `loadQuoteForStudio`, source labels     | 0057, 0059               |                                                                     |
| `calculator`         | Quote Studio persisted-quote path (with `commercialV2`)                             | 0057                     | estimator itself is pure                                            |
| `workIntake`         | `lib/work/actions.ts`, `lib/nextActions/actions.ts`, intake forms                   | 0058                     |                                                                     |
| `acceptanceGate`     | `lib/delivery/actions.ts`, `lib/delivery/load.ts`, `planChoiceOpenV2`, portal pages | 0060                     | existing gate callers still use `planChoiceOpen`                    |
| `integrationWorkers` | webhook branch, `capture`, `enqueue`, `runOnce`, `processGoCardlessEvent`, cron     | 0062–0065                | pair with `billingActivation`; sandbox provider only                |
| `billingActivation`  | `lib/billing/{obligations,exceptions,activation,mandates}.ts`                       | 0062, 0064, 0065 (+0059) |                                                                     |

With all flags off every legacy route, action, webhook, column and trigger behaves as on `main`; the three legacy clients are untouched by code. Full detail: `docs/admin-redesign/phase5-runbook.md`.

## 4. Migrations — NOT APPLIED anywhere

Apply order from the dry-run (ascending; detected dependencies precede dependants): **0057 → 0058 → 0059 → 0060 → 0062 → 0063 → 0064 (after 0059) → 0065 (after 0062)**. 0056 (PR #20, applied in production as `client_economics`) has no file on this branch; 0061 is unassigned.

| File                                   | Adds                                                                                                                                                | Touches existing                                                                                                                                           |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0057_opportunities_quotes.sql`        | `opportunities`, `quotes`, `quote_versions`, `quote_approvals`; frozen-content and separation triggers                                              | nothing                                                                                                                                                    |
| `0058_next_actions_work_class.sql`     | enums `work_class`, `coverage_decision`; `client_next_actions`                                                                                      | `issues` + 4 nullable columns                                                                                                                              |
| `0059_service_arrangements.sql`        | `service_arrangements`, `service_schedules`, `handover_schedules`, `supersede_service_arrangement()`, frozen-content trigger, two client-safe views | `order_forms.commercial_version` (default `v1`), `contract_acceptances` + 3 nullable snapshot columns                                                      |
| `0060_build_acceptance_checklists.sql` | `build_acceptances`, `checklist_tasks`, `handover_tasks`; two SECURITY DEFINER portal functions                                                     | nothing                                                                                                                                                    |
| `0062_billing_obligations.sql`         | `billing_obligations`, `provider_payments`, `payment_allocations`, ceiling trigger                                                                  | `invoices.obligation_id`; **backfill** one obligation per live legacy build invoice; replaces `invoices_one_build_per_project` with two equivalent indexes |
| `0063_integration_operations.sql`      | `integration_events`, `integration_operations`, `integration_operations_claim()` (service role only; RLS with no policies)                          | nothing                                                                                                                                                    |
| `0064_service_activations.sql`         | `mandates`, `service_activations`, guard trigger                                                                                                    | `subscriptions` + 3 nullable columns and two partial unique indexes on new-model rows only                                                                 |
| `0065_finance_exceptions.sql`          | `finance_exceptions`, append-only guard                                                                                                             | nothing                                                                                                                                                    |

Every file: additive, RLS on, staff-only or service-only policies, integer minor units + currency, timestamptz, idempotent DDL, header with purpose / dependencies / NOT APPLIED / rollback. Cross-file FKs deliberately omitted for `arrangement_id` (0060, 0062) and `activation_id` (0065) until both sides are in the ledger. Ledger is name-based and not reproducible from the directory (Phase 0 §11) — read `schema_migrations` first (N-h). Dry-run: `pnpm -C apps/web migrations:dry-run`. Invariants: `apps/web/scripts/legacy-invariants.sql`.

## 5. Tests

`pnpm -C apps/web test` — 57 files, **854 tests passed** (17 September 2026, after Phase 5). Redesign files and counts: `next-fixtures` 7, `next-ops-fixtures` 17, `estimator` 31, `commercial-quotes` 29, `work-classify` 20, `next-actions` 12, `arrangements` 46, `acceptance-gate` 15, `checklists` 18, `activation` 41, `obligations` + `reconciliation` 60, `integration-ops` 25. All pure logic, in-memory stores and fake providers; no DOM, no database. Typecheck clean; eslint clean on every owned file.

**Suggested but not committed** (their tasks did not own `apps/web/tests/`): `next-finance.test.ts` (7), `next-agreements.test.ts` (11), `next-portal.test.ts` (9) — the bodies are in the Phase 1 docs and pass when run from a scratchpad. `packages/db/src/rls.test.mjs` still skips without `SUPABASE_DB_URL`.

## 6. Screenshots still to take

No agent had browser access. Owed (brief §13 matrix: 320 / 390 / 768 / 1440 px and 200 % zoom, light shell not applicable — dark only):

- `/admin/next`, `/admin/next/clients`, `/admin/next/clients/[id]`, `/admin/next/quotes`, `/admin/next/quotes/q-northline-v2`, `/admin/next/quotes/q-atlas-v1` (insufficient-confidence state);
- Finance: overview, invoices `?filter=overdue`, `invoices/INV-1033-02`, collections `?filter=authorised`, `collections/COL-1037-2026-10`, subscriptions, reconciliation, exceptions, `exceptions/EX-0004`;
- Agreements: library with an empty filter result, `brightwell-of-v1`, `northline-of-v1`, `orbit-hs-v1`, `legacy-terms-2026-02`;
- Sales `?tab=pipeline&view=board`, `?tab=catalogue`, `opportunities/opp-northline`; Delivery `?tab=queue`, `projects/proj-westbridge`, `intake?req=req-morrow-33`; Automations `?preview=auto-activation`; Settings `?section=integrations`, `?section=flags`;
- Portal preview: `?client=brightwell&role=billing`, `?client=harbour` next-steps, `?client=orbit` next-steps, `?client=cedar`, `&wide=1`;
- real `/portal/checklist` and `/portal/acceptance` as a client (flag off);
- every `loading.tsx` skeleton and one `error.tsx` per area (throw from a page in a preview to capture).

## 7. What was NOT done

- **Phase 6** — not started.
- **Applying any migration** — none applied to any database (branch or production); no ledger reconciliation performed; `packages/db/src/types.ts` not regenerated.
- **Deployment** — nothing pushed; `feat/admin-redesign` is local; no preview deployment; no PR.
- **Browser verification** — no route was rendered in a browser by any Phase 1–5 agent; the Phase 0 prototype slice was rendered once (1440 / pane / 375 px). No Phase 2–4 flag-on path has been exercised end to end against a database.
- **Live provider tests** — no GoCardless, Stripe, Xero or Resend call; the integration tests use fakes. `GOCARDLESS_ENVIRONMENT`'s value is unknown; the Xero connector could not be reached in Phase 0.
- **Wiring not done** (each named in its phase doc): Finance pages to `obligationBalance` / `suggestMatch` / exception actions; agreements pages to the Phase 3 actions; portal accept surface for schedules; Quote Studio `[id]` calling `loadQuoteForStudio` first (the listing deliberately does not link database rows); Delivery queue → intake link; client workspace next-action strip → `listNextActions`; existing plan-gate callers → `planChoiceOpenV2`; staff activation approval action that reserves and enqueues `gocardless.schedule_activation`; manual bank-transfer recording; Stripe webhook onto the inbox; `PortalHeader` entries for the new portal routes; `packages/content/package.json` export for `orderFormV2`; `Reference` on Xero payment PUTs; the `provider_payments.environment` vocabulary (`test|live` vs `sandbox|live`) alignment.
- **Legacy defects carried forward** (Phase 0): MFA redirect loop (N-a / `a426c87` not cherry-picked), eleven hub actions without `requireStaff`, open redirects, `deleteClient` hard-delete, client-readable `audit_log`, the Stripe card rail, page-load Xero reconciliation on the legacy billing pages, `ensureClientWorkspace` auto-join on GET.
- **Capability model** (18.11), signatory/billing/project client roles, staff read policy on the inbox/outbox — not built.
- **Suggested Phase 1 tests** not committed (see §5).
- **Prices** — nothing is approved: catalogue items draft/sandbox, estimator policy draft, £600 tax basis pending.

## 8. Next steps for Louis (in order)

1. **Read** `docs/admin-redesign/phase5-pilot-checklist.md` and decide which §18 items you can close now; record each in the decision register with evidence. 18.11 (capabilities) and 18.12 (pilot cohort) gate everything else.
2. **Review the prototype visually**: `pnpm -C apps/web dev`, sign in as staff, walk the routes in §2 with every flag off; take the §6 screenshots; note design changes before any wiring.
3. **Hotfix the legacy auth defects** on a separate branch from `main`: cherry-pick `a426c87` (N-a), add `requireStaff()` to the eleven hub actions and `openLead`, validate `?next=`. These are independent of the redesign and should ship first.
4. **Reconcile the migration ledger** (N-h): list `supabase_migrations.schema_migrations`, decide the fate of `060`/`0056`, the two `0020`s and the `0052` names; add any unknown names to the dry-run's ledger list; re-run `pnpm -C apps/web migrations:dry-run`.
5. **Rehearse on a Supabase branch database**: run `legacy-invariants.sql` for the three protected tenants (before), apply 0057–0065 in the dry-run order, run it again (after), diff — only the documented 0062/0059 fields may differ. Exercise the 0059 triggers/function, the 0062 pre-checks, the 0062/0064 unique indexes and the 0065 guard by hand.
6. **Preview deployment** of `feat/admin-redesign` with flags off (no migration needed) for the visual review; then a second preview pointed at the branch database with `OPS_V2_FLAGS=commercialV2,workIntake,acceptanceGate` to try the Phase 2–3 flag-on paths with two staff users and one fictional client tenant; `integrationWorkers,billingActivation` only with a **sandbox** GoCardless webhook.
7. **Commit the suggested Phase 1 tests**, regenerate `packages/db/src/types.ts` after the branch apply, and do the small wiring items in §7 that the review confirms.
8. **Merge to `main` and deploy with flags off** only after 2, 3 and the review — production behaviour is unchanged by the merge alone, but the merge is not covered by the routine website-release approval and needs its own go-ahead (checklist D6). Applying migrations to production (D3) is a separate, later decision.
9. **Pilot** one approved new client (B1) on one journey with the flags in checklist B2; keep TDE, STL and NFT on the legacy paths; observe at least one real exception through to evidence-backed resolution before enabling any further automation.

## 9. Where things are

- Brief: `~/Documents/Codex/2026-09-13/referenced-chatgpt-conversation-this-is-an/outputs/Nullshift-Admin-Redesign-Developer-Brief.md`
- Phase 0 report and decision register: `docs/ADMIN-REDESIGN-PHASE0-2026-09-17.md`
- Phase docs: `docs/admin-redesign/phase1-*.md`, `phase2-*.md`, `phase3-*.md`, `phase4-*.md`, `phase5-*.md`
- Prototype: `apps/web/app/admin/(dashboard)/next/**`, fixtures `apps/web/lib/next/fixtures*.ts`, shell CSS `next.module.css` / `ops.module.css`
- Logic: `apps/web/lib/{estimator,commercial,work,nextActions,legal,delivery,billing,integrations}/`, `apps/web/lib/flags.ts`, `apps/web/lib/planGate.ts`, `apps/web/lib/xeroOps.ts`
- Migrations: `supabase/migrations/0057–0065` (NOT APPLIED); readiness scripts: `apps/web/scripts/migration-dry-run.mjs`, `apps/web/scripts/legacy-invariants.sql`
- Runbook / permissions / approvals: `docs/admin-redesign/phase5-runbook.md`, `phase5-permissions-audit.md`, `phase5-pilot-checklist.md`; operations note in `docs/OPERATIONS.md` ("Admin redesign v2 (flags off)"); migration note in `supabase/README.md`.
