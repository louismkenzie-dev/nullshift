auto-install-peers=true
strict-peer-dependencies=false
# Next.js + Supabase + Stripe ship their own bundled deps; allow hoisting so the
# Next compiler can resolve them across the workspace.
shamefully-hoist=false
node-linker=isolated
